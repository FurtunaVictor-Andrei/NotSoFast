# NotSoFast
Project that simulate autobreaking safety system using RC car and android application.
Upload the arduino code to your Arduino IDE and connect the components as they should 
For the Android Studio app:
1. Change android manifest and mainactivity with my code
2. In android studio folder manager (left side) go to res > right click > new resources directory > right click > new resources file .xml
and add my code
